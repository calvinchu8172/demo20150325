<h1>Edit Event</h1>

<%= form_for @event, :url => event_path(@event), :method => :patch do |f| %>
    <%= render 'form', :f => f %>
    <%= f.submit "Update" %>
<% end %>

<p><%= link_to 'Back to index', events_path %></p>